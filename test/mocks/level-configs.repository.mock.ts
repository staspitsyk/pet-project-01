import { createMock } from '@golevelup/ts-jest';

import { LevelConfigsRepository } from 'src/modules/level-configs/level-configs.repository';

export const LevelConfigsRepositoryMock = createMock<LevelConfigsRepository>();
